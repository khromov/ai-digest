{
  "name": "agg-cli",
  "version": "1.0.0",
  "description": "CLI tool to aggregate files into a single Markdown file",
  "main": "dist/index.js",
  "bin": {
    "agg-cli": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "ts-node src/index.ts",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["cli", "aggregate", "markdown"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^14.14.31",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.2"
  },
  "dependencies": {
    "commander": "^7.1.0",
    "glob": "^7.1.6",
    "ignore": "^5.1.8"
  }
}
